{{#if virtualScrollbar}}
  {{#ember-scrollable
    classNames='lt-scrollable'
    autoHide=autoHide
    horizontal=horizontal
    vertical=vertical
    scrollToY=scrollTo
    onScrollToY=onScrollToY
    as |scrollbar|
  }}
    {{yield}}
  {{/ember-scrollable}}
{{else}}
  {{yield}}
{{/if}}
