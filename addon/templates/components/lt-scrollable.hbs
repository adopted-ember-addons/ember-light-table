{{#if virtualScrollbar}}
  {{#ember-scrollable
    classNames='lt-scrollable'
    autoHide=autoHide
    scrollTo=scrollTo
    onScroll=onScroll
    as |scrollbar|
  }}
    {{yield}}
  {{/ember-scrollable}}
{{else}}
  {{yield}}
{{/if}}
