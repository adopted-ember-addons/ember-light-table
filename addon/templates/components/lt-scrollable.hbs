{{#if virtual}}
  {{#ember-scrollable
    classNames=classNames
    autoHide=autoHide
    horizontal=horizontal
    vertical=vertical
    scrollToY=_scrollTopSet
    onScrollY=(action 'onScroll')
  }}
    {{yield (hash scrollTop=_scrollTopGet onScrollTo=(action 'onScrollTo'))}}
  {{/ember-scrollable}}
{{else}}
  {{#lt-standard-scrollable
    height=height
    classNames=classNames
    scrollToY=_scrollTopSet
    onScroll=(action 'onScroll')
  }}
    {{yield (hash scrollTop=_scrollTopGet onScrollTo=(action 'onScrollTo'))}}
  {{/lt-standard-scrollable}}
{{/if}}
