import { Column } from 'ember-light-table';
import { module, test } from 'qunit';

module('Unit | Classes | Column');

// Replace this with your real tests.
test('it works', function(assert) {
  let subject = new Column();
  assert.ok(subject);
});
