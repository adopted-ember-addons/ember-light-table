import Table from 'ember-light-table';
import { module, test } from 'qunit';

module('Unit | Classes | Table');

// Replace this with your real tests.
test('it works', function(assert) {
  let subject = new Table();
  assert.ok(subject);
});
